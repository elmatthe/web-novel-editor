# Webnovel Editor — Python dependencies
# Pinned to exact versions resolved at first successful install (Phase 1, 2026-06-21,
# Python 3.12.10). Per AI-WORKSPACE conventions, every package is pinned with ==.
# The startup scripts install from this file (scripts/requirements.txt).
#
# tkinter is bundled with standard Python on Windows/macOS and is NOT pip-installable;
# it is intentionally absent here. The app checks for it at startup.
#
# PyPDF2 was removed in Phase 6: nothing imports it yet. It is pinned ONLY when the
# deferred orphan-single-heading-page removal feature is built (see BRIEFING deferred
# features). Re-add `PyPDF2==3.0.1` (or migrate to the maintained `pypdf`) at that time.

pdfplumber==0.11.10
reportlab==5.0.0
pytest==9.1.1
# natsort: natural (numeric-aware) filename ordering for the folder-scan input mode
# (1,2,10 not 1,10,2). Latest stable verified on PyPI 2026-07-17 (Plan 1 Phase 1).
natsort==8.4.0
tomli==2.4.1
# Official local Ollama client. Imported lazily only when the Ollama provider is selected.
ollama==0.6.2
# Official Google Gen AI SDK — the current `google-genai`, NOT the deprecated
# `google-generativeai`. Requires Python >= 3.10, matching config.toml python_minimum.
# Imported lazily only when the Gemini provider builds a client (ai/providers/gemini.py);
# nothing in the app imports it at package load, so the suite and the whole script-only
# path still run with this package absent. Latest stable verified on PyPI 2026-07-24
# (Plan 2b Phase 2); the Phase 0 candidate 2.14.0 is still current and is now pinned.
google-genai==2.14.0
# Official Groq SDK. Imported lazily only when the Groq provider builds a client
# (ai/providers/groq.py); nothing in the app imports it at package load, so the suite
# and the whole script-only path still run with this package absent. Requires Python
# >= 3.10, matching config.toml python_minimum.
#
# WHY 1.6.0, re-checked on PyPI 2026-07-25 (Plan 2b Phase 3). Phase 0 flagged 1.6.0 as a
# SAME-DAY release and refused to pin it blind. Re-checked a day later: 1.6.0 is still
# the latest, no 1.6.1 hotfix followed, and nothing was yanked. Its changelog entry over
# 1.5.0 is repository infrastructure only — CI runner configuration in the workflow
# templates, plus CODEOWNERS — with no client-code or breaking change, so its runtime
# diff against the month-settled 1.5.0 is effectively nil. Pinning back to 1.5.0 would
# have bought no extra soak time on the code that actually ships while giving up a
# month of upstream API-schema updates.
groq==1.6.0
