"""Profiles package — per-novel data (canonical names, forced fixes).

Profile data is consumed by the universal rules; it is not rule logic itself.
Shadow Slave is the first profile and the reference implementation.
"""
